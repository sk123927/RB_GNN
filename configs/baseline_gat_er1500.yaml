data:
  data_root: data
  dataset: trigger/er1500
  seed: 123

model:
  model: gat
  hidden_dim: 128
  num_layers: 2
  dropout: 0.3

training:
  epochs: 200
  batch_size: 4
  lr: 0.001
  focal_alpha: 0.9
  focal_gamma: 4.0
  device: auto

